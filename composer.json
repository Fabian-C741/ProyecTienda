{
    "name": "tienda-online/multi-tenant",
    "description": "Plataforma multi-tenant de e-commerce",
    "type": "project",
    "require": {},
    "scripts": {
        "post-install-cmd": [
            "[ -f backend/composer.json ] && cd backend && /usr/local/bin/composer install --no-dev --optimize-autoloader --no-interaction || true"
        ],
        "post-update-cmd": [
            "[ -f backend/composer.json ] && cd backend && /usr/local/bin/composer install --no-dev --optimize-autoloader --no-interaction || true"
        ]
    }
}
